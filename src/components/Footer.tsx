import { MenuBar } from '@/components';

export const Footer = () => {
  return (
    <footer className="flex w-full items-center justify-center px-4 border-t border-gray-200">
      <MenuBar />
    </footer>
  );
};
